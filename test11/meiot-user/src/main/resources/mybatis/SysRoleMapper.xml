<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.meiot.mapper.SysRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.meiot.entity.SysRole">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="status" property="status" />
        <result column="type" property="type" />
        <result column="pid" property="pid" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="belong_user_id" property="belongUserId" />
        <result column="is_super_admin" property="isSuperAdmin" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, status1, type, pid, create_time, update_time,is_super_admin
    </sql>

    <select id="selectTypeById" resultType="int" parameterType="int">
        SELECT type FROM  sys_role WHRER id = #{id} limit 1

    </select>




    <select id="queryNamesById" resultType="java.lang.String">
        SELECT name FROM  sys_role where
        id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}

        </foreach>

    </select>

</mapper>
