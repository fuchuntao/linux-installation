<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.meiot.mapper.TroubleTicketUserDao">

    <resultMap type="cn.meiot.entity.TroubleTicketUser" id="TroubleTicketUserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="alarmId" column="alarm_id" jdbcType="INTEGER"/>
        <result property="deviceAlias" column="device_alias" jdbcType="VARCHAR"/>
        <result property="switchAlias" column="switch_alias" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TroubleTicketUserMap">
        select
          id, user_id, alarm_id, device_alias, switch_alias
        from meiot_aftersale.trouble_ticket_user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TroubleTicketUserMap">
        select
          id, user_id, alarm_id, device_alias, switch_alias
        from meiot_aftersale.trouble_ticket_user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TroubleTicketUserMap">
        select
          id, user_id, alarm_id, device_alias, switch_alias
        from meiot_aftersale.trouble_ticket_user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="alarmId != null">
                and alarm_id = #{alarmId}
            </if>
            <if test="deviceAlias != null and deviceAlias != ''">
                and device_alias = #{deviceAlias}
            </if>
            <if test="switchAlias != null and switchAlias != ''">
                and switch_alias = #{switchAlias}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into meiot_aftersale.trouble_ticket_user(user_id, alarm_id, device_alias, switch_alias)
        values (#{userId}, #{alarmId}, #{deviceAlias}, #{switchAlias})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update meiot_aftersale.trouble_ticket_user
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="alarmId != null">
                alarm_id = #{alarmId},
            </if>
            <if test="deviceAlias != null and deviceAlias != ''">
                device_alias = #{deviceAlias},
            </if>
            <if test="switchAlias != null and switchAlias != ''">
                switch_alias = #{switchAlias},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from meiot_aftersale.trouble_ticket_user where id = #{id}
    </delete>

</mapper>